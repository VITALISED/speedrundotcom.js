import { Structure } from "../../base/structure";
import type { PersonalBestData } from "../../typings/data/users";

export class PersonalBest extends Structure {
    constructor(data: PersonalBestData)
}